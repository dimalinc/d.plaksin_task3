package elements.baseicelements;

import elements.BaseElement;
import org.openqa.selenium.By;

public class Image extends BaseElement {
    public Image(By locator, String elementName) {
        super(locator, elementName);
    }
}
